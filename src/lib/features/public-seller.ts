/**
 * Public self-serve seller listing (vendor dashboard CTAs + /dashboard/draftsman).
 * When false, only admins list plans via Admin → แบบบ้าน.
 * Flip to true to reopen public seller onboarding.
 */
export const PUBLIC_SELLER_SELF_LISTING_ENABLED = false;
